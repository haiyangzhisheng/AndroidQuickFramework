<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aaagame.proframework">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <!-- 自定义BaseApplication -->
    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.T_MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- 下拉刷新上拉加载 -->
        <activity android:name=".activity.T_RefreshTwinkling_Activity" />
        <!-- 轮播图 -->
        <activity android:name=".activity.T_Banner_Activity" />
        <!-- 图片查看 -->
        <activity android:name=".imagebrowser.ImagePagerActivity" />
        <!-- 选项卡 -->
        <activity android:name=".activity.T_SmartTabActivity" />
        <!-- 模板Activity -->
        <activity android:name=".activity.T_Sample_Activity" />
        <!-- 一般软件中帮助说明界面 -->
        <activity android:name=".activity.T_HelpActivity" />
        <!-- 获取和设置主题颜色 -->
        <activity android:name=".activity.T_PaletteActivity" />
        <!-- xutils3用法 -->
        <activity android:name=".activity.T_Xutils_Activity" />
        <!-- 应用后台下载服务 -->
        <service android:name=".activity.AppUpdateService" />
        <!-- 拍摄和选择照片 -->
        <activity android:name=".activity.T_CameraActivity" />
        <!--列表为空时显示为空界面-->
        <activity android:name=".activity.T_ListEmpty_Activity" />
        <!--软件Activity管理-->
        <activity android:name=".activity.T_AppManager_Activity" />


        <!--支付-->
        <activity android:name=".activity.T_Pay_Activity" />
        <!-- 微信支付 -->
        <activity
            android:name=".wxapi.WXPayEntryActivity"
            android:exported="true"
            android:launchMode="singleTop" />
        <!-- 微信分享 -->
        <!-- alipay sdk begin -->

        <activity
            android:name="com.alipay.sdk.app.H5PayActivity"
            android:configChanges="orientation|keyboardHidden|navigation|screenSize"
            android:exported="false"
            android:screenOrientation="behind"
            android:windowSoftInputMode="adjustResize|stateHidden" />
        <activity
            android:name="com.alipay.sdk.app.H5AuthActivity"
            android:configChanges="orientation|keyboardHidden|navigation"
            android:exported="false"
            android:screenOrientation="behind"
            android:windowSoftInputMode="adjustResize|stateHidden" />

        <!-- alipay sdk end -->
        <!--银联支付-->
        <activity
            android:name="com.unionpay.UPPayWapActivity"
            android:configChanges="orientation|keyboardHidden"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize" />
        <!--微信分享-->
        <activity
            android:name=".wxapi.WXEntryActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <!-- 图片剪切 -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
    </application>

</manifest>